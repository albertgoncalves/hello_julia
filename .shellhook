#!/usr/bin/env bash

if [ $(uname -s) = "Darwin" ]; then
    alias ls="ls --color=auto"
    alias ll="ls -l"
else
    alias open="xdg-open"
fi

if [ ! -d pkgs/ ]; then
    mkdir pkgs
fi

export JULIA_DEPOT_PATH=$(realpath pkgs/)
julia pkgs.jl
